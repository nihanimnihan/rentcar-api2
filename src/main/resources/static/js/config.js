// Global runtime config — edit in deployment with real keys
window.RC_CONFIG = window.RC_CONFIG || {};
// Set your publishable Google Maps browser key here in production
window.RC_CONFIG.GOOGLE_MAPS_API_KEY = window.RC_CONFIG.GOOGLE_MAPS_API_KEY || '';
